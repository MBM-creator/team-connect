/**
 * QA is intentionally retained in the codebase while it is unavailable in Team Connect.
 * Set this to true to restore the existing UI, pages and API routes.
 */
export const QA_ENABLED: boolean = false;

/**
 * The editable stages panel is intentionally retained while it is unavailable
 * on the job overview. Set this to true to restore the existing panel and its
 * stage-management requests.
 */
export const JOB_STAGES_SECTION_ENABLED: boolean = false;
